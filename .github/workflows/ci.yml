name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]
  release:
    types: [created]

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  PYTHONDONTWRITEBYTECODE: 1

jobs:
  lint:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-lint-${{ hashFiles('rust_engine/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Install pre-commit
        run: pip install pre-commit

      - name: pre-commit cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/pre-commit
          key: pre-commit-${{ hashFiles('.pre-commit-config.yaml') }}

      - name: Run all pre-commit hooks
        run: pre-commit run --all-files

  test-python:
    name: Python Tests (${{ matrix.os }} / py${{ matrix.python-version }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]
        python-version: ["3.11", "3.12"]

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          cache: pip

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-${{ hashFiles('rust_engine/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-

      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: rust_engine/target
          key: ${{ runner.os }}-rust-target-${{ hashFiles('rust_engine/src/**/*.rs', 'rust_engine/Cargo.toml') }}
          restore-keys: ${{ runner.os }}-rust-target-

      - name: Create venv & add to PATH
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Install Python deps
        run: pip install maturin pydantic pyyaml typer rich pytest pytest-cov

      - name: Build Rust engine
        run: maturin develop --release

      - name: Run tests
        run: python -m pytest tests/unit/ tests/integration/ -v --tb=short --cov=credweaver --cov-report=xml

      - name: Upload coverage
        if: matrix.os == 'ubuntu-latest' && matrix.python-version == '3.12'
        uses: codecov/codecov-action@v4
        with:
          file: coverage.xml
          fail_ci_if_error: false

  test-rust:
    name: Rust Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust_engine/target
          key: ${{ runner.os }}-rust-${{ hashFiles('rust_engine/Cargo.lock') }}

      - name: Rustfmt check
        run: cargo fmt --manifest-path rust_engine/Cargo.toml -- --check

      - name: Clippy
        run: cargo clippy --manifest-path rust_engine/Cargo.toml -- -D clippy::all -A dead_code -A unused_imports -A unused_variables

      - name: Cargo test
        run: cargo test --manifest-path rust_engine/Cargo.toml --release

  build-wheel:
    name: Build Wheel (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust_engine/target
          key: ${{ runner.os }}-wheel-${{ hashFiles('rust_engine/Cargo.lock') }}

      - name: Build wheel
        run: pip install maturin && maturin build --release --out dist/

      - name: Upload wheel artifact
        uses: actions/upload-artifact@v4
        with:
          name: wheel-${{ matrix.os }}
          path: dist/*.whl
          retention-days: 7

  docker:
    name: Docker Build & Smoke Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          load: true # export image to local Docker daemon so docker run works
          tags: credweaver:ci
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Smoke test — help
        run: docker run --rm credweaver:ci --help

      - name: Smoke test — strategies list
        run: docker run --rm credweaver:ci strategies list

      - name: Smoke test — generate from profile
        run: |
          mkdir -p /tmp/credweaver-output && chmod 777 /tmp/credweaver-output
          docker run --rm \
            -v ${{ github.workspace }}/profiles:/home/credweaver/profiles:ro \
            -v /tmp/credweaver-output:/output \
            credweaver:ci generate \
              --profile /home/credweaver/profiles/ci_smoke.yaml \
              --preset fast \
              --output /output/test_wordlist.txt \
              --no-stats
          wc -l /tmp/credweaver-output/test_wordlist.txt

      - name: Login to GHCR (on release)
        if: github.event_name == 'release'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to GHCR (on release)
        if: github.event_name == 'release'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:${{ github.ref_name }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-security-${{ hashFiles('rust_engine/Cargo.lock') }}

      - name: Install bandit
        run: pip install bandit[toml]

      - name: Bandit Python security scan
        run: bandit -r credweaver/ -ll --skip B101 -f json -o bandit-report.json || true

      - name: Show bandit results
        run: |
          python3 -c "
          import json, sys
          data = json.load(open('bandit-report.json'))
          issues = data.get('results', [])
          if issues:
              print(f'Found {len(issues)} security issues:')
              for i in issues:
                  print(f'  [{i[\"issue_severity\"]}] {i[\"issue_text\"]} ({i[\"filename\"]}:{i[\"line_number\"]})')
          else:
              print('No security issues found.')
          "

      # Use taiki-e/install-action to download a pre-built cargo-audit binary.
      # This is far faster than `cargo install cargo-audit` (no compilation).
      - name: Install cargo-audit
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit

      - name: Cargo audit
        run: cd rust_engine && cargo audit

  benchmark:
    name: Performance Benchmark
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: stable

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            rust_engine/target
          key: ${{ runner.os }}-bench-${{ hashFiles('rust_engine/Cargo.lock') }}

      - name: Create venv & add to PATH
        run: |
          python -m venv .venv
          echo "$PWD/.venv/bin" >> $GITHUB_PATH

      - name: Install deps and build
        run: |
          pip install maturin pydantic pyyaml typer rich
          maturin develop --release

      - name: Run benchmark
        id: benchmark
        run: |
          python3 -c "
          import time, json
          from credweaver.core.profile import Profile, DateInfo
          from credweaver.config.loader import load_config, merge_config
          from credweaver.core.engine import Engine

          profile = Profile(
              name='john', surname='doe', nickname='johnny',
              birthdate=DateInfo(day=15, month=6, year=1990),
              pet_name='rex', company='acme', keywords=['security'],
          )
          results = {}
          for label, use_rust in [('python', False), ('rust', True)]:
              cfg = load_config()
              cfg = merge_config(cfg, {'generation': {'use_rust_engine': use_rust, 'max_depth': 3}})
              engine = Engine(config=cfg)
              start = time.perf_counter()
              count = sum(1 for _ in zip(range(100_000), engine.generate(profile)))
              elapsed = time.perf_counter() - start
              results[label] = {'count': count, 'elapsed': round(elapsed, 3), 'speed': round(count/elapsed)}
          print(json.dumps(results))
          " > benchmark_results.json
          cat benchmark_results.json

      - name: Post benchmark comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const results = JSON.parse(fs.readFileSync('benchmark_results.json', 'utf8'));
            const py = results.python;
            const rs = results.rust;
            const speedup = (rs.speed / py.speed).toFixed(1);
            const body = `## Benchmark Results\n\n| Engine | Passwords | Time (s) | Speed (pass/s) |\n|--------|-----------|----------|----------------|\n| Python only | ${py.count.toLocaleString()} | ${py.elapsed} | ${py.speed.toLocaleString()} |\n| Python + Rust | ${rs.count.toLocaleString()} | ${rs.elapsed} | ${rs.speed.toLocaleString()} |\n\nSpeedup: **${speedup}x**`;
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body
            });
